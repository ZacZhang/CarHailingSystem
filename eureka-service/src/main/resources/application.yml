spring:
  application:
    name: eureka-server

server:
  port: 8761

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    withTimeInMsWhenSyncEmpty: 0

logging:
  level:
    com.netflix: 'WARN'
    org.springframework.cloud: 'WARN'