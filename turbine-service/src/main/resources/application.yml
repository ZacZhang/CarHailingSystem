server:
  port: 8098

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'
      base-path: /

turbine:
  app-config: account-service, location-service, dispatch-service
  cluster-name-expression: new String('default')
